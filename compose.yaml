services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=task_management_sys'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5432'
